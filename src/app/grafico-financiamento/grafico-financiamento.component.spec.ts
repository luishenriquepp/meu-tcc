/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GraficoFinanciamentoComponent } from './grafico-financiamento.component';

describe('Component: GraficoFinanciamento', () => {
  it('should create an instance', () => {
    let component = new GraficoFinanciamentoComponent();
    expect(component).toBeTruthy();
  });
});
